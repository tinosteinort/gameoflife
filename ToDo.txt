[X] Use maven-release-plugin
    -> mit maven-versions-plugin ersetzt
[X] Use Spring for DI
[ ] Use AMapper
[ ] Board Painter als eigene Componente
[ ] JNLP

* use long instead of int for x/y of Cell
* EndlessBoard not endless, but width/height = Long.MAX_VALUE
* use BigInteger instead of int / long